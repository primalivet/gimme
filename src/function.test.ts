import { pipe, flow } from './function'

describe('pipe', () => {
  test.todo('applies a value to a set of functions in sequence, explicit')
})
describe('flow', () => {
  test.todo('applies a value to a set of function in sequence, pointfree')
})
